RewriteEngine On

# Allow direct access to real files/dirs (uploads, etc.)
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Route all API requests to index.php
RewriteRule ^(.*)$ index.php [QSA,L]

